import NotesContainer from "../Note/NotesContainer";

import classes from "./Main.module.scss";

const Main = () => {
  return (
    <main>
      <NotesContainer />
    </main>
  );
};

export default Main;
